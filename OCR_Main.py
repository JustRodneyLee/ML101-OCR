from scipy import misc
from matplotlib.pyplot import imread
import numpy as np

def preprocess(self, src):
    name = src.split('\\').
    img = imread()

class ForwardLayer:
    def __init__(self):
        
    def 

def main():
    hiddenLayers = []
    hiddenLayers.append(ForwardLayer())
    if os.path.isfile("OCR_en_US_weights.csv") & os.path.isfile("OCR_en_US_biases.csv"):
        hiddenLayers[0].load(np.loadtxt("OCR_en_US_weights.csv",delimiter=","),np.loadtxt("OCR_en_US_biases.csv",delimiter=","))
    while True:
        print("Enter path of image:")
        path = input()
        #preprocess the image of that path
        #train through the neural network
        #print result
        

if __name__ == "__main__":
    main()
